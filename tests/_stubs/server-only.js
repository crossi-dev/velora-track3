// Stub for the Next.js `server-only` marker package.
// Real `server-only` throws when imported from a client component; in
// tests we don't have that distinction, so it's a no-op.
module.exports = {};
